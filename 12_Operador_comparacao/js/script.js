/**
 *  == igual
 *  === idêntico
 *  != diferente de 
 *  > maior que
 *  < menor que
 *  >= maior igual que
 *  <= menor igual que
 */

var meta = 3000;
var atual = 2970;

if(meta != atual ) {
    console.log("Você ainda não atingiu sua meta.");
}

if(atual > 3000) {
    console.log("Parabéns! Você ultrapassou a meta.");
}